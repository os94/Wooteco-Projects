package racing.view;

import org.junit.jupiter.api.Test;

import static org.assertj.core.api.Assertions.assertThat;

public class InputViewTest {

    // Test code for private method InputView.isRightCarName()
    /*@Test
    void 자동차_목록_입력_예외처리() {
        assertThat(InputView.isRightCarName("pobi,sean")).isEqualTo(true);
        assertThat(InputView.isRightCarName("pppeee,sean")).isEqualTo(false);
        assertThat(InputView.isRightCarName(",pobi,sean")).isEqualTo(false);
        assertThat(InputView.isRightCarName("pob12,sean")).isEqualTo(false);
        assertThat(InputView.isRightCarName("pobi,sean,")).isEqualTo(false);
        assertThat(InputView.isRightCarName("pobi,,sean")).isEqualTo(false);

    }*/
}
